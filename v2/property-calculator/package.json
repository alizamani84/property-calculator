{
  "name": "swedish-property-calculator",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npm run build:calc && npm run build:method",
    "build:calc": "VITE_ENTRY=calculator npx vite build && cp ../dist/index.html ../calculator.html",
    "build:method": "VITE_ENTRY=methodology npx vite build && cp ../dist/methodology.html ../methodology.html",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
